# Registro de decisiones metodológicas

Este archivo es el índice estructurado de decisiones del proyecto. Los
documentos enlazados contienen la justificación completa; la configuración
versionada contiene los valores que ejecuta el código. Una decisión `frozen`
no se modifica después de consultar el test bloqueado: cualquier alternativa
se implementa como sensibilidad o como nueva versión del protocolo.

## Estados

- `frozen`: decisión primaria aprobada e implementada.
- `provisional`: se usa en el demo, pendiente de auditoría antes de MIMIC-IV completo.
- `pending`: no debe asumirse ni implementarse silenciosamente.

## Decisiones

| ID | Estado | Decisión primaria | Implementación/configuración | Evidencia y sensibilidades |
|---|---|---|---|---|
| D001 | frozen | MIMIC-IV Demo v2.2 para desarrollo local; MIMIC-IV v3.1 previsto para el análisis completo. | `scripts/download_mimic_demo.py`; manifiestos de artefactos | `docs/mimic_code_sofa_plan.md` |
| D002 | provisional | Primera estancia UCI elegible por ingreso para el análisis primario de cohorte. | `src/mimic_sepsis/cohort.py`; `scripts/audit_cohort_policy_freeze.py` reconstruye y compara las tres políticas | Todas las estancias y primera por paciente serán sensibilidades completas; requiere firma clínica/metodológica. |
| D003 | frozen | Infección sospechada: antimicrobiano primero + cultivo en 24 h, o cultivo primero + antimicrobiano en 72 h; `t_si` es el evento anterior. | `src/mimic_sepsis/infection.py` | `docs/sepsis_definition.md` |
| D004 | provisional | Antimicrobiano sistémico confirmado por primera administración EMAR; cultivo de sangre en la definición primaria. | `config/antimicrobial_rules.csv`; `scripts/audit_antimicrobial_rules.py`; `scripts/build_demo_sofa_incremental.py` | Prescripción y cultivos expandidos serán sensibilidades. El paquete estático hace auditable la lista, pero aún requiere revisión clínica antes del análisis completo. |
| D005 | frozen | SOFA horario: peor valor por componente en `(e-24 h, e]`; ausentes a cero en el total MIMIC y total completo conservado aparte. | `src/mimic_sepsis/sofa_*.py` | `docs/mimic_code_sofa_plan.md` |
| D006 | frozen | Baseline Sepsis-3: mínimo SOFA total horario en `[t_si-48 h, t_si)`; cero explícitamente marcado si no hay hora basal. | `config/sepsis3.json`; `src/mimic_sepsis/sepsis_labels.py` | Sensibilidad exigiendo baseline observado. |
| D007 | frozen | Ventana aguda `[t_si-24 h, t_si+24 h]`; `t0` es el primer cruce de incremento SOFA ≥2. | `config/sepsis3.json`; `src/mimic_sepsis/sepsis_labels.py` | `docs/sofa_infection_integration.md` |
| D008 | frozen | Separar inicio estimado (`t0`) de disponibilidad retrospectiva: `label_available_at=max(t0,t_si_confirmed_at)`. | `src/mimic_sepsis/sepsis_labels.py` | Ambos tiempos se auditan; ninguno se usa como predictor. |
| D009 | frozen | Landmarks horarios desde 6 h de UCI; horizonte primario de 6 h y lookback de 24 h. | `config/landmarks.json`; `src/mimic_sepsis/landmarks.py` | Horizontes 3/12/24 h y lookbacks 6/12 h. |
| D010 | provisional | Proxy primario de shock: Sepsis-3, vasopresor y lactato >2 mmol/L concurrentes ±6 h y dentro de ±24 h de `t0`; la resucitación adecuada no se infiere y se marca como no verificada. | `config/septic_shock.json`; `src/mimic_sepsis/septic_shock.py` | Exigir fluidos documentados y/o MAP <65 será sensibilidad; requiere revisión clínica. |
| D011 | pending | Umbral de cobertura SOFA para declarar un episodio evaluable en el análisis principal. | `scripts/audit_phenotype_freeze.py` compara baseline observado/cobertura, describe completitud en `t0` y audita artefactos separados recalculados con `sofa_complete` | Debe congelarse tras revisión clínica/estadística y antes del conjunto completo; el demo no elige la regla. |
| D012 | frozen | Outcome futuro en `(L,L+H]`: un evento en `L` es prevalente; uno exactamente al final cuenta. Seguimiento corto sin evento se censura, no se convierte en control. | `config/landmarks.json`; `src/mimic_sepsis/landmarks.py` | La convención alternativa `[L,L+H)` queda descartada para evitar coexistencia ambigua. |
| D013 | frozen | Los signos vitales están disponibles en `charttime`; los laboratorios solo en `storetime` y se atribuyen a la estancia por el `charttime` de la muestra. Las ventanas predictoras son `[L-W,L)`. | `config/features.json`; `src/mimic_sepsis/feature_sources.py`; `src/mimic_sepsis/features.py` | Un laboratorio sin `storetime` no se backdata ni entra como predictor. |
| D014 | provisional | Conjunto inicial de diez variables para validar ingeniería: frecuencia cardiaca, MAP, frecuencia respiratoria, SpO2, temperatura, leucocitos, creatinina, bilirrubina, plaquetas y lactato. | `config/features.json` | La selección final, transformaciones y complejidad deben congelarse antes del test completo. |
| D015 | frozen | Development, validation y test se separan por `subject_id`; los artefactos son físicamente distintos y los notebooks de desarrollo no abren test. Todo acceso se registra. En datos no-demo, test ni siquiera se materializa por defecto. | `config/splits.json`; `src/mimic_sepsis/splits.py`; `docs/test_access_log.md` | El test del demo es una prueba de ingeniería, no una evaluación ciega. |
| D016 | provisional | El baseline clínico usa cinco resúmenes preespecificados de las últimas 24 h; la regresión imputa y escala dentro de cada fold. Los landmarks se ponderan para que cada paciente tenga igual peso total. | `config/modeling.json`; `src/mimic_sepsis/modeling.py`; notebook 10 | El demo solo valida ingeniería. Variables, penalización y ponderación se congelarán con el conjunto completo antes de abrir su test. |
| D017 | frozen | La incertidumbre y las comparaciones entre modelos remuestrean `subject_id` completos. Las diferencias son pareadas sobre las mismas réplicas; se intentan 1.000 réplicas con semilla fija. | `config/evaluation.json`; `src/mimic_sepsis/evaluation.py`; notebook 11 | Réplicas sin ambas clases producen métricas de discriminación no definidas y se contabilizan, no se sustituyen. |
| D018 | frozen | Las sensibilidades técnicas se ejecutan sobre la cuadrícula de horizontes 3/6/12/24 h y lookbacks 6/12/24 h, siempre sin test. Se comparan además las políticas de estancia de forma estructural. | `config/sensitivity.json`; `src/mimic_sepsis/sensitivity.py`; notebook 12 | En el análisis completo cada política de cohorte requerirá reconstrucción de extremo a extremo; los resultados del demo no seleccionan configuración. |
| D019 | frozen | Las credenciales PhysioNet se usan solo para descarga HTTPS interactiva. PostgreSQL es una instalación separada y cada sesión se fuerza a read-only con timeout; ambos backends deben pasar preflight sin leer filas clínicas. | `src/mimic_sepsis/db.py`; `src/mimic_sepsis/deployment.py`; `docs/full_deployment_guide.md` | El pipeline pandas del demo está prohibido sobre los CSV completos; se requiere backend SQL o chunked. |
| D020 | frozen | La ruta para los CSV completos usa DuckDB fuera de memoria para reducir fuentes y después lotes pandas delimitados por estancias para SOFA, etiquetas, landmarks y features. Cada parte tiene manifiesto, hash y reanudación verificable; la procedencia encadena los hashes de entradas. | `src/mimic_sepsis/chunked_*.py`; `scripts/build_full_pipeline_chunked.py` | Todo el recorrido se validó por igualdad de esquema y multiconjunto de filas frente al demo monolítico; falta medir rendimiento en el servidor completo. |
| D021 | frozen | Antimicrobianos, EMAR y cultivos se restringen a los ingresos de la cohorte antes de formar pares. Se conservan pares no calificantes dentro de esa población para auditoría, pero no infecciones de ingresos fuera del estudio. | `scripts/build_demo_sofa_incremental.py`; `src/mimic_sepsis/full_extract.py` | La armonización eliminó 53 pares extramuros del demo sin modificar las 25 estancias Sepsis-3 ni las etiquetas de shock. |
| D022 | frozen | La portabilidad monolítico→particionado exige igualdad de esquema y de multiconjunto de filas, no solo recuentos. El verificador está limitado explícitamente a MIMIC-IV Demo 2.2 para no abrir el test completo. | `src/mimic_sepsis/equivalence.py`; `scripts/verify_demo_chunked_equivalence.py` | Verificados los descriptores comunes de cohorte, SOFA, cuatro artefactos de etiquetas, seis de landmarks y seis de features con cero diferencias bidireccionales. |
| D023 | frozen | La planificación de información para modelos binarios seguirá los criterios de Riley (precisión del riesgo, shrinkage y optimismo), contando grados de libertad candidatos. No se sustituye por una regla fija de eventos por variable; los landmarks se auditan además como clústeres de paciente. | `config/sample_size.json`; `src/mimic_sepsis/information_audit.py`; `docs/clinical_freeze_dossier.md` | El cálculo formal permanece bloqueado mientras falte el Cox–Snell R² anticipado y hasta disponer de recuentos de development completo. |
| D024 | frozen | Una ejecución no-demo debe superar una puerta de protocolo por fase. Para fenotipado exige D002, D004, D010 y D011 congeladas; modelo añade D014, D016 y D027; test exige además D029 y el contrato D031. Ausencia o estado distinto de `frozen` bloquea, nunca se interpreta como aprobación. | `config/protocol_status.json`; `src/mimic_sepsis/deployment.py`; `scripts/preflight_protocol.py` | El demo queda exento solo para ingeniería. Toda firma debe sincronizar acta, registro y estado ejecutable. |
| D025 | frozen | La evaluación pre-test añade calibración logística, métricas binarias con frontera `p≥umbral`, carga/episodios de alertas, warning time dentro del horizonte y decision-curve por landmark. Los umbrales 0,02–0,20 son solo una rejilla exploratoria; ningún umbral es operativo hasta definir acción y trade-off clínicos. | `config/evaluation.json`; `src/mimic_sepsis/evaluation.py`; notebook 13 | La incertidumbre de métricas y calibración remuestrea pacientes completos. `operating_threshold_probabilities` permanece vacío y el test cerrado. |
| D026 | frozen | Edad, género registrado, raza administrativa, tipo/origen de ingreso, seguro y primera unidad UCI se conservan como descriptores de auditoría, no como predictores primarios. Subgrupos con alguna clase <10 y una celda complementaria se suprimen; métricas requieren al menos 20 eventos y 20 no eventos. | `config/subgroups.json`; `src/mimic_sepsis/subgroups.py`; notebook 13 | Las categorías administrativas no se interpretan como constructos biológicos. El demo debe producir principalmente estimaciones suprimidas/no reportables. |
| D027 | provisional | El comparador no lineal técnico es gradient boosting por histogramas sobre las mismas cinco variables del baseline, con imputación e indicadores dentro de fold, ponderación por paciente y early stopping interno desactivado. | `config/modeling.json`; `src/mimic_sepsis/modeling.py`; notebook 13 | Los hiperparámetros del demo no seleccionan modelo. Espacio de tuning, complejidad y modelo final requieren firma estadística tras la planificación de información y antes del test. |
| D028 | frozen | La disponibilidad se informa por predictor y partición. La carga por landmark se agrupa en 0, 1 y ≥2 predictores ausentes y se usa solo para auditoría descriptiva de rendimiento. Toda celda positiva <10 pacientes o landmarks se suprime. | `src/mimic_sepsis/missingness.py`; `src/mimic_sepsis/reporting.py`; notebook 13 | La imputación sigue ajustándose dentro de fold. El demo valida el contrato, no cuantifica transportabilidad ni justifica excluir pacientes. |
| D029 | provisional | Si el modelo congelado requiere recalibración, se ajustará una regresión logística de intercepto y pendiente exclusivamente en validation, con peso total igual por paciente. Se exigen al menos 100 pacientes con evento y 100 sin evento; nunca se ajusta en test. | `config/calibration.json`; `src/mimic_sepsis/recalibration.py`; notebook 13 | El umbral de información requiere firma estadística. El demo queda bloqueado y no produce coeficientes de recalibración. |
| D030 | frozen | Materializar test no-demo requiere la puerta de protocolo `test`, una especificación versionada y completa con estado `frozen`, y una autorización local de roles clínico y estadístico ligada por SHA-256 a ese modelo y a la versión de datos. | `src/mimic_sepsis/model_freeze.py`; `src/mimic_sepsis/test_access.py`; `scripts/prepare_model_freeze.py`; `scripts/build_full_pipeline_chunked.py`; ejemplos de `config/` | El preparador solo produce un borrador no autorizante y enumera bloqueos. La especificación final fija commit, run, outcome, horizonte, variables, modelo, parámetros, calibración, umbrales, decisiones y hash del informe de validation. El acceso se anota antes de ejecutar; Demo 2.2 queda exento solo para ingeniería. |
| D031 | frozen | El informe pre-test puede persistirse y reutilizarse solo como tablas agregadas, sin identificadores ni tiempos individuales, con checksums por tabla y SHA-256 global ligado a artefactos fuente, configuración e implementación. | `src/mimic_sepsis/report_artifacts.py`; `scripts/validate_pretest_report.py`; notebook 13 | La carga revalida contenido, snapshot de configuración sin claves sensibles, privacidad y hash. El SHA global es la referencia del futuro `model_freeze.json`; los derivados permanecen ignorados por Git. |
| D032 | frozen | El perfil i5/32 GiB limita todas las conexiones DuckDB a 8 GB y dos hilos, usa lotes iniciales de 100 estancias, normaliza/enlaza/poda fuentes de features una vez por lote antes de pandas, vectoriza el agregador temporal, proyecta para el informe solo horizonte y predictores primarios, y ejecuta el bootstrap agrupado mediante pesos de frecuencia equivalentes a duplicar pacientes. Cada ejecución completa persiste telemetría agregada por etapa y debe superar una puerta de RSS ≤24 GiB, ausencia de swap del proceso, completitud y coincidencia del perfil; las partes clínicas vacías conservan su esquema. | `config/compute_32gb.json`; `src/mimic_sepsis/duckdb_runtime.py`; `src/mimic_sepsis/resource_telemetry.py`; `src/mimic_sepsis/resource_validation.py`; `src/mimic_sepsis/feature_sources_sql.py`; `src/mimic_sepsis/features.py`; `src/mimic_sepsis/pretest_inputs.py`; `src/mimic_sepsis/resource_planning.py`; `src/mimic_sepsis/evaluation.py` | `docs/hardware_32gb.md`; equivalencia exacta sobre las seis matrices del demo, benchmark local por etapa, equivalencia sintética automatizada y preflight agregado. La primera ejecución completa debe validar `resource_report.json`; si falla por RSS puede reducirse el lote sin cambiar el método. |

## Proceso de cambio

Cada nueva decisión recibe un identificador estable. Un cambio debe actualizar
en el mismo commit: este registro, el documento clínico correspondiente, la
configuración ejecutable, las pruebas de frontera y la versión/esquema de los
artefactos afectados.
